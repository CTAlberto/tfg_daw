package com.tfg.backend.repository;

import com.tfg.backend.entity.TipoCantidad;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ITipoCantidadRepository extends JpaRepository<TipoCantidad, Long> {
}
